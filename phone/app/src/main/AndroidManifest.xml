<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="andytran.dmap_phone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ChooseEMILYActivity"
            android:label="@string/title_activity_choose">
            android:label="@string/title_activity_choose">
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="MainActivity"></activity>
        <activity
            android:name=".ClientSocketTestActivity"
            android:label="Test Socket"></activity>
        <activity
            android:name=".ImageManagerActivity"
            android:label="@string/title_activity_add_to_app_db">

            <!-- Move the intent filter to Choose Emily activity after finishing -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestUploadActivity"
            android:exported="true"></activity>
    </application>

</manifest>
