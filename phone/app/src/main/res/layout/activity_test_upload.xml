<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="andytran.dmap_phone.TestUploadActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GetGraphics"
        android:id="@+id/try_btn"
        android:layout_marginTop="41dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Flush"
        android:id="@+id/flush_btn"
        android:layout_below="@+id/try_btn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="46dp" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageview_test"
        android:layout_below="@+id/flush_btn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="146dp" />

    <Button
        android:id="@+id/check_db"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Check Database"
        android:layout_below="@+id/flush_btn"
        android:layout_centerHorizontal="true"
        />
</RelativeLayout>
